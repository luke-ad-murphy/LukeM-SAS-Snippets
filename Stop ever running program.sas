* If your SAS programme is a bit mixed up (keep running when it is supposed to be finished), then run this line - ;


*);*/;/*'*/ /*"*/; %mend;run; quit;;;;;;;;


*The code is for the following

Unclosed macro definition
Unclosed comments
Unclosed steps
Unfinished steps
Run-group procedures (e.g. PROC SQL) not closed with QUIT;
